// AI Service has been removed from the application.
// This file is kept as a placeholder to prevent import errors if it's referenced elsewhere, 
// though all references should be gone.

export const evaluateLoanPurpose = async (purpose: string) => {
  return { status: 'Success', reason: 'Manual approval' };
};
